version: "2"
services:
  server:
    image: pbl2_nuvem
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "7853:7853/tcp"
      - "7873:7873/tcp"
    network_mode: "bridge"